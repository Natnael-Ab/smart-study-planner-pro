function MainLayout({ children }) {
      return (
          <div>
                <nav>Navbar</nav>
                      <main>{children}</main>
                          </div>
                            );
                            }

                            export default MainLayout;
